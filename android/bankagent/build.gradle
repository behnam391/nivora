apply plugin: 'com.android.application'
apply plugin: 'org.jetbrains.kotlin.android'

android {
    namespace 'ir.nivora.bankagent'
    compileSdk 36
    defaultConfig {
        applicationId 'ir.nivora.bankagent'
        minSdk 26
        targetSdk 36
        versionCode 2
        versionName '1.0.1'
    }
    def releaseStore = System.getenv('NIVORA_KEYSTORE_PATH')
    def releaseStorePassword = System.getenv('NIVORA_KEYSTORE_PASSWORD')
    def releaseKeyAlias = System.getenv('NIVORA_KEY_ALIAS')
    def releaseKeyPassword = System.getenv('NIVORA_KEY_PASSWORD')
    signingConfigs {
        if (releaseStore && releaseStorePassword && releaseKeyAlias && releaseKeyPassword) {
            nivoraRelease {
                storeFile file(releaseStore);storePassword releaseStorePassword;keyAlias releaseKeyAlias;keyPassword releaseKeyPassword
                enableV1Signing true;enableV2Signing true;enableV3Signing true;enableV4Signing true
            }
        }
    }
    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            if (signingConfigs.findByName('nivoraRelease')) signingConfig signingConfigs.nivoraRelease
        }
    }
    compileOptions { sourceCompatibility JavaVersion.VERSION_17; targetCompatibility JavaVersion.VERSION_17 }
    kotlinOptions { jvmTarget = '17' }
}
