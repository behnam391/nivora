(()=>{let rows=[];function mount(){document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="/admin-topups.css">');document.querySelector('nav').insertAdjacentHTML('beforeend','<button class="nav" id="topups-nav">شارژ کیف پول <i id="topups-badge">۰</i></button>');document.querySelector('main').insertAdjacentHTML('beforeend',`<section id="wallet-topups" class="view hidden"><div class="topup-toolbar"><div><h2>درخواست‌های شارژ کیف پول</h2><p class="muted">بررسی رسید مشتری و شارژ خودکار کیف پول</p></div><select id="topup-filter"><option value="all">همه</option><option value="under_review">در انتظار بررسی</option><option value="approved">تأییدشده</option><option value="rejected">ردشده</option></select></div><div id="topup-list" class="topup-list"></div></section>`);$('#topups-nav').onclick=open;$('#topup-filter').onchange=render;refreshBadge()}
async function refreshBadge(){try{const pending=await api('/api/admin/wallet-topups?status=under_review');$('#topups-badge').textContent=fa(pending.length)}catch{}}
async function open(){document.querySelectorAll('.view').forEach(v=>v.classList.add('hidden'));document.querySelectorAll('.nav').forEach(n=>n.classList.remove('active'));$('#wallet-topups').classList.remove('hidden');$('#topups-nav').classList.add('active');$('#page-title').textContent='شارژ کیف پول';rows=await api('/api/admin/wallet-topups');render()}
function render(){const f=$('#topup-filter').value,data=rows.filter(x=>f==='all'||x.status===f),labels={under_review:'در انتظار',approved:'تأییدشده',rejected:'ردشده'};$('#topup-list').innerHTML=data.length?data.map(x=>`<article class="topup-row"><div><b>${esc(x.customer_name)}</b><small>${esc(x.phone)}</small></div><div><b>${fa(x.amount_toman)} تومان</b><small>موجودی فعلی: ${fa(x.balance_toman)}</small></div><div>${x.receipt_image_url?`<a class="receipt" target="_blank" href="${esc(x.receipt_image_url)}">تصویر رسید</a>`:esc(x.receipt_reference||'—')}</div><div><span class="status ${x.status}">${labels[x.status]}</span><small>${date(x.created_at)}</small></div><div class="topup-actions">${x.status==='under_review'?`<button class="primary approve-topup" data-id="${x.id}">تأیید</button><button class="danger reject-topup" data-id="${x.id}">رد</button>`:''}</div></article>`).join(''):'<div class="empty">درخواستی وجود ندارد.</div>';document.querySelectorAll('.approve-topup').forEach(b=>b.onclick=()=>review(b.dataset.id,'approve'));document.querySelectorAll('.reject-topup').forEach(b=>b.onclick=()=>review(b.dataset.id,'reject'))}
async function review(id,action){const note=prompt(action==='approve'?'یادداشت تأیید (اختیاری)':'علت رد درخواست')||'';if(action==='reject'&&!note)return;await api(`/api/admin/wallet-topups/${id}/${action}`,{method:'POST',body:JSON.stringify({note,reviewedBy:'مدیر'})});toast(action==='approve'?'کیف پول شارژ شد':'درخواست رد شد');rows=await api('/api/admin/wallet-topups');render();refreshBadge()}
document.addEventListener('DOMContentLoaded',mount)})();
