(()=>{
  const $=selector=>document.querySelector(selector);
  const token=()=>sessionStorage.getItem('nivora_admin_token')||'';
  const esc=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
  const fa=value=>new Intl.NumberFormat('fa-IR').format(Number(value)||0);
  let locations=[],active=null,endpoints=[],editingEndpoint=null;

  async function api(path,options={}){
    const response=await fetch(path,{...options,headers:{authorization:`Bearer ${token()}`,'content-type':'application/json'}});
    const data=await response.json().catch(()=>({}));
    if(!response.ok){const error=new Error(data.error||'خطا');error.code=data.error;throw error}
    return data;
  }
  function toast(message){const node=$('#toast');node.textContent=message;node.classList.add('show');setTimeout(()=>node.classList.remove('show'),2200)}

  function mount(){
    document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="/admin-locations.css">');
    $('nav').insertAdjacentHTML('beforeend','<button class="nav" id="locations-nav">لوکیشن‌ها</button>');
    $('main').insertAdjacentHTML('beforeend','<section id="locations" class="view hidden"><div class="panel-head standalone"><div><h2>لوکیشن‌ها و مسیرهای اتصال</h2><p>سرور، ظرفیت و ساب‌دامین‌های جایگزین هر لوکیشن</p></div><button id="new-location" class="primary">+ لوکیشن جدید</button></div><div id="locations-grid" class="locations-grid"></div></section>');
    document.body.insertAdjacentHTML('beforeend',dialogs());
    $('#locations-nav').onclick=openView;$('#new-location').onclick=()=>openEdit();
    document.querySelectorAll('.close-location').forEach(button=>button.onclick=()=>$('#location-dialog').close());
    document.querySelectorAll('.close-location-plans').forEach(button=>button.onclick=()=>$('#location-plans-dialog').close());
    document.querySelectorAll('.close-endpoints').forEach(button=>button.onclick=()=>$('#endpoints-dialog').close());
    document.querySelectorAll('.close-endpoint-edit').forEach(button=>button.onclick=()=>$('#endpoint-dialog').close());
    $('#location-form').onsubmit=save;$('#location-plans-form').onsubmit=savePlans;$('#endpoint-form').onsubmit=saveEndpoint;
    $('#delete-location').onclick=remove;$('#new-endpoint').onclick=()=>openEndpointEdit();
  }

  function dialogs(){return `
    <dialog id="location-dialog"><form id="location-form"><div class="modal-head"><div><p class="eyebrow">LOCATION</p><h2 id="location-title">لوکیشن جدید</h2></div><button type="button" class="icon close-location">×</button></div><input id="location-id" type="hidden"><div class="form-grid"><label>عنوان نمایشی<input id="location-name" placeholder="فنلاند — هلسینکی" required></label><label>کد کشور<input id="location-country" maxlength="2" placeholder="FI" required></label><label>شهر<input id="location-city"></label><label>ارائه‌دهنده / دیتاسنتر<input id="location-provider"></label><label>نوع پنل<input id="location-panel" value="3x-ui"></label><label>شماره Inbound<input id="location-inbound" type="number" min="1"></label><label>ظرفیت فروش (۰ = نامحدود)<input id="location-capacity" type="number" min="0" value="0"></label><label class="check"><input id="location-active" type="checkbox" checked> قابل فروش</label></div><div class="modal-actions"><button type="button" id="delete-location" class="danger">حذف</button><button type="button" class="ghost close-location">انصراف</button><button class="primary">ذخیره</button></div></form></dialog>
    <dialog id="location-plans-dialog"><form id="location-plans-form"><div class="modal-head"><div><p class="eyebrow">PLAN ROUTING</p><h2 id="location-plans-title">پلن‌های لوکیشن</h2></div><button type="button" class="icon close-location-plans">×</button></div><div id="location-plans-list" class="plans-checklist"></div><div class="modal-actions"><button type="button" class="ghost close-location-plans">انصراف</button><button class="primary">ذخیره اتصال پلن‌ها</button></div></form></dialog>
    <dialog id="endpoints-dialog" class="wide-dialog"><div class="dialog-body"><div class="modal-head"><div><p class="eyebrow">SMART ROUTES</p><h2 id="endpoints-title">مسیرهای اتصال</h2></div><button type="button" class="icon close-endpoints">×</button></div><div class="dns-note"><b>تنظیم Cloudflare</b><span>برای Reality رکوردهای A/CNAME را روی <strong>DNS only</strong> بگذارید. هر مسیر می‌تواند فعلاً به همین IP و بعداً به یک سرور مستقل اشاره کند.</span></div><div class="endpoint-toolbar"><p>اپ Nivora از میان مسیرهای فعال، سریع‌ترین مسیر در دسترس را انتخاب می‌کند.</p><button type="button" id="new-endpoint" class="primary">+ مسیر جدید</button></div><div id="endpoints-list" class="endpoints-list"></div></div></dialog>
    <dialog id="endpoint-dialog"><form id="endpoint-form"><div class="modal-head"><div><p class="eyebrow">SUBDOMAIN ROUTE</p><h2 id="endpoint-title">مسیر جدید</h2></div><button type="button" class="icon close-endpoint-edit">×</button></div><input id="endpoint-id" type="hidden"><div class="form-grid"><label>نام مسیر<input id="endpoint-label" placeholder="مسیر یک" required></label><label>ساب‌دامین یا IP<input id="endpoint-host" dir="ltr" placeholder="fi1.nivorali.com" required></label><label>پورت<input id="endpoint-port" type="number" min="1" max="65535" value="443" required></label><label>اولویت<input id="endpoint-priority" type="number" min="0" max="10000" value="10" required></label><label class="check span-2"><input id="endpoint-active" type="checkbox" checked> در خروجی اشتراک فعال باشد</label></div><p class="modal-note">عدد اولویت کمتر، بالاتر نمایش داده می‌شود. انتخاب نهایی در اپ با تست سرعت انجام می‌شود.</p><div id="endpoint-error" class="error"></div><div class="modal-actions"><button type="button" id="delete-endpoint" class="danger">حذف</button><button type="button" class="ghost close-endpoint-edit">انصراف</button><button class="primary">ذخیره مسیر</button></div></form></dialog>`}

  async function openView(){
    document.querySelectorAll('.view').forEach(view=>view.classList.add('hidden'));document.querySelectorAll('.nav').forEach(nav=>nav.classList.remove('active'));
    $('#locations').classList.remove('hidden');$('#locations-nav').classList.add('active');$('#page-title').textContent='مدیریت لوکیشن‌ها';await load();
  }
  async function load(){
    locations=await api('/api/admin/locations');
    $('#locations-grid').innerHTML=locations.length?locations.map(location=>`<article class="location-card ${location.active?'':'off'}"><div class="location-head"><div class="flag">${esc(location.country_code)}</div><div><h3>${esc(location.name)}</h3><p>${esc(location.city||'—')} · ${esc(location.provider||'بدون ارائه‌دهنده')}</p></div></div><div class="location-meta"><div><small>پنل / Inbound</small><b>${esc(location.panel_type)} / ${esc(location.panel_inbound_id||'—')}</b></div><div><small>ظرفیت</small><b>${location.capacity?fa(location.capacity):'نامحدود'}</b></div><div><small>پلن متصل</small><b>${fa(location.plan_count)}</b></div><div><small>مسیر آنلاین</small><b>${fa(location.online_endpoint_count)} از ${fa(location.endpoint_count)}</b></div></div><div class="location-actions three"><button class="ghost edit-location" data-id="${location.id}">ویرایش</button><button class="ghost location-plans" data-id="${location.id}">پلن‌ها</button><button class="primary location-endpoints" data-id="${location.id}">مسیرها</button></div></article>`).join(''):'<div class="panel empty">هنوز لوکیشنی ساخته نشده است.</div>';
    document.querySelectorAll('.edit-location').forEach(button=>button.onclick=()=>openEdit(button.dataset.id));document.querySelectorAll('.location-plans').forEach(button=>button.onclick=()=>openPlans(button.dataset.id));document.querySelectorAll('.location-endpoints').forEach(button=>button.onclick=()=>openEndpoints(button.dataset.id));
  }
  function openEdit(id=null){active=id?locations.find(location=>location.id===id):null;$('#location-title').textContent=active?'ویرایش لوکیشن':'لوکیشن جدید';$('#location-id').value=active?.id||'';$('#location-name').value=active?.name||'';$('#location-country').value=active?.country_code||'';$('#location-city').value=active?.city||'';$('#location-provider').value=active?.provider||'';$('#location-panel').value=active?.panel_type||'3x-ui';$('#location-inbound').value=active?.panel_inbound_id||'';$('#location-capacity').value=active?.capacity||0;$('#location-active').checked=active?.active??true;$('#delete-location').classList.toggle('hidden',!active);$('#location-dialog').showModal()}
  async function save(event){event.preventDefault();const id=$('#location-id').value,body={name:$('#location-name').value,countryCode:$('#location-country').value,city:$('#location-city').value,provider:$('#location-provider').value,panelType:$('#location-panel').value,panelInboundId:Number($('#location-inbound').value)||null,capacity:Number($('#location-capacity').value),active:$('#location-active').checked};await api(id?`/api/admin/locations/${id}`:'/api/admin/locations',{method:id?'PATCH':'POST',body:JSON.stringify(body)});$('#location-dialog').close();toast('لوکیشن ذخیره شد');await load()}
  async function openPlans(id){active=locations.find(location=>location.id===id);const rows=await api(`/api/admin/locations/${id}/plans`);$('#location-plans-title').textContent=`پلن‌های ${active.name}`;$('#location-plans-list').innerHTML=rows.map(plan=>`<label class="plan-check"><input type="checkbox" data-id="${plan.id}" ${plan.attached?'checked':''}>${esc(plan.name)}</label>`).join('');$('#location-plans-dialog').showModal()}
  async function savePlans(event){event.preventDefault();const planIds=[...document.querySelectorAll('#location-plans-list input:checked')].map(input=>input.dataset.id);await api(`/api/admin/locations/${active.id}/plans`,{method:'POST',body:JSON.stringify({planIds})});$('#location-plans-dialog').close();toast('اتصال پلن‌ها ذخیره شد');await load()}
  async function remove(){if(!active||!confirm('لوکیشن حذف شود؟'))return;try{await api(`/api/admin/locations/${active.id}`,{method:'DELETE'});$('#location-dialog').close();await load()}catch(error){alert(error.code==='LOCATION_IN_USE'?'ابتدا اتصال پلن‌های این لوکیشن را بردارید.':'حذف انجام نشد.')}}

  async function openEndpoints(id){active=locations.find(location=>location.id===id);$('#endpoints-title').textContent=`مسیرهای ${active.name}`;$('#endpoints-dialog').showModal();await loadEndpoints()}
  async function loadEndpoints(){endpoints=await api(`/api/admin/locations/${active.id}/endpoints`);renderEndpoints()}
  function health(endpoint){if(endpoint.health_status==='online')return `<span class="endpoint-health online">آنلاین · ${fa(endpoint.last_latency_ms)} ms</span>`;if(endpoint.health_status==='offline')return '<span class="endpoint-health offline">در دسترس نیست</span>';return '<span class="endpoint-health unknown">تست نشده</span>'}
  function renderEndpoints(){
    $('#endpoints-list').innerHTML=endpoints.length?endpoints.map(endpoint=>`<article class="endpoint-row ${endpoint.active?'':'disabled'}"><div class="route-priority">${fa(endpoint.priority)}</div><div class="route-main"><b>${esc(endpoint.label)}</b><code dir="ltr">${esc(endpoint.host)}:${fa(endpoint.port)}</code></div><div>${health(endpoint)}<small>Cloudflare: DNS only</small></div><div class="route-actions"><button class="ghost test-endpoint" data-id="${endpoint.id}">تست مسیر</button><button class="ghost edit-endpoint" data-id="${endpoint.id}">ویرایش</button></div></article>`).join(''):'<div class="empty"><b>هنوز مسیر جایگزینی تعریف نشده</b>برای شروع دو یا سه ساب‌دامین DNS only اضافه کنید.</div>';
    document.querySelectorAll('.test-endpoint').forEach(button=>button.onclick=()=>testEndpoint(button));document.querySelectorAll('.edit-endpoint').forEach(button=>button.onclick=()=>openEndpointEdit(button.dataset.id));
  }
  function openEndpointEdit(id=null){editingEndpoint=id?endpoints.find(endpoint=>endpoint.id===id):null;$('#endpoint-title').textContent=editingEndpoint?'ویرایش مسیر':'مسیر جدید';$('#endpoint-id').value=editingEndpoint?.id||'';$('#endpoint-label').value=editingEndpoint?.label||'';$('#endpoint-host').value=editingEndpoint?.host||'';$('#endpoint-port').value=editingEndpoint?.port||443;$('#endpoint-priority').value=editingEndpoint?.priority??((endpoints.length+1)*10);$('#endpoint-active').checked=editingEndpoint?.active??true;$('#delete-endpoint').classList.toggle('hidden',!editingEndpoint);$('#delete-endpoint').onclick=deleteEndpoint;$('#endpoint-error').textContent='';$('#endpoint-dialog').showModal()}
  async function saveEndpoint(event){event.preventDefault();const id=$('#endpoint-id').value,body={label:$('#endpoint-label').value,host:$('#endpoint-host').value,port:Number($('#endpoint-port').value),priority:Number($('#endpoint-priority').value),active:$('#endpoint-active').checked};try{await api(id?`/api/admin/location-endpoints/${id}`:`/api/admin/locations/${active.id}/endpoints`,{method:id?'PATCH':'POST',body:JSON.stringify(body)});$('#endpoint-dialog').close();toast('مسیر اتصال ذخیره شد');await loadEndpoints();await load()}catch(error){$('#endpoint-error').textContent=error.code==='ENDPOINT_ALREADY_EXISTS'?'این ساب‌دامین و پورت قبلاً ثبت شده است.':'اطلاعات مسیر معتبر نیست.'}}
  async function testEndpoint(button){button.disabled=true;button.textContent='در حال تست…';try{const result=await api(`/api/admin/location-endpoints/${button.dataset.id}/test`,{method:'POST',body:'{}'});toast(result.status==='online'?`مسیر آنلاین است؛ ${fa(result.latencyMs)} میلی‌ثانیه`:'مسیر در دسترس نیست');await loadEndpoints();await load()}finally{button.disabled=false}}
  async function deleteEndpoint(){if(!editingEndpoint||!confirm(`مسیر ${editingEndpoint.label} حذف شود؟`))return;await api(`/api/admin/location-endpoints/${editingEndpoint.id}`,{method:'DELETE'});$('#endpoint-dialog').close();toast('مسیر حذف شد');await loadEndpoints();await load()}

  document.addEventListener('DOMContentLoaded',mount);
})();
