(()=>{
  const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  let nodes=[],tunnels=[],editing=null;
  function mount(){
    $('#admin-nav').insertAdjacentHTML('beforeend','<button class="nav" id="tunnels-nav" data-icon="⇄">تونل ایران</button>');
    $('main').insertAdjacentHTML('beforeend',`<section id="tunnels" class="view hidden"><div class="panel-head standalone"><div><h2>تونل ورودی ایران</h2><p>مسیر ورودی داخل ایران را به خروجی خارج متصل و وضعیتش را از همین‌جا مدیریت کن.</p></div><button id="new-tunnel" class="primary">+ تعریف تونل</button></div><div class="safety-note">فعال‌کردن این گزینه به‌تنهایی تونل سیستم‌عامل را نصب نمی‌کند. ابتدا نصب امن روی هر دو سرور انجام می‌شود، سپس آدرس ورودی و وضعیت آن اینجا ثبت و آزمایش می‌شود.</div><div id="tunnels-grid" class="plans-grid"></div></section>`);
    document.body.insertAdjacentHTML('beforeend',`<dialog id="tunnel-dialog"><form id="tunnel-form"><div class="modal-head"><div><p class="eyebrow">IRAN TRANSIT</p><h2 id="tunnel-title">تونل جدید</h2></div><button type="button" class="icon close-tunnel">×</button></div><input id="tunnel-id" type="hidden"><div class="form-grid"><label>نام مسیر<input id="tunnel-name" required placeholder="ورودی ایران ← فنلاند"></label><label>نوع تونل<select id="tunnel-transport"><option value="amneziawg">AmneziaWG (پیشنهادی)</option><option value="wireguard">WireGuard استاندارد</option><option value="reverse_tcp">Reverse TCP fallback</option></select></label><label>سرور ورودی ایران<select id="tunnel-entry" required></select></label><label>سرور خروجی خارج<select id="tunnel-exit" required></select></label><label>IP/دامنه ورودی برای مشتری<input id="tunnel-host" dir="ltr" required placeholder="ir-edge.example.com"></label><label>پورت عمومی<input id="tunnel-port" type="number" min="1" max="65535" value="51820" required></label><label>MTU<input id="tunnel-mtu" type="number" min="1100" max="1420" value="1280" required><small>برای موبایل ایران از ۱۲۸۰ شروع کن.</small></label><label class="check"><input id="tunnel-active" type="checkbox"> پس از نصب، فعال باشد</label><label class="span-2">یادداشت<textarea id="tunnel-note" rows="3" placeholder="اپراتورها، تاریخ نصب، مسیر خروجی یا محدودیت‌ها"></textarea></label></div><div id="tunnel-error" class="error"></div><div class="modal-actions"><button type="button" id="tunnel-delete" class="danger">حذف</button><button type="button" class="ghost close-tunnel">انصراف</button><button class="primary">ذخیره</button></div></form></dialog>`);
    $('#tunnels-nav').onclick=open;$('#new-tunnel').onclick=()=>edit();$$('.close-tunnel').forEach(b=>b.onclick=()=>$('#tunnel-dialog').close());$('#tunnel-form').onsubmit=save;$('#tunnel-delete').onclick=remove;
  }
  const $$=s=>[...document.querySelectorAll(s)];
  async function open(){document.querySelectorAll('.view').forEach(v=>v.classList.add('hidden'));document.querySelectorAll('.nav').forEach(v=>v.classList.remove('active'));$('#tunnels').classList.remove('hidden');$('#tunnels-nav').classList.add('active');$('#page-title').textContent='تونل ایران';await load()}
  async function load(){[nodes,tunnels]=await Promise.all([api('/api/admin/panel-nodes'),api('/api/admin/transit-tunnels')]);$('#tunnels-grid').innerHTML=tunnels.length?tunnels.map(t=>`<article class="plan-card ${t.active?'':'off'}"><span class="status ${t.health_status==='online'?'approved':t.health_status==='offline'?'rejected':'awaiting_receipt'}">${t.health_status==='online'?'آنلاین':t.health_status==='offline'?'خارج از دسترس':'آزمایش نشده'}</span><h3>${esc(t.name)}</h3><p>${esc(t.entry_node_name)} ← ${esc(t.exit_node_name)}</p><p dir="ltr" class="muted">${esc(t.public_host)}:${t.public_port}</p><ul><li>${esc(t.transport)}</li><li>MTU ${t.mtu}</li><li>${t.last_latency_ms==null?'—':`${t.last_latency_ms} ms`}</li></ul><footer><button class="ghost test-tunnel" data-id="${t.id}">تست ورودی</button><button class="ghost edit-tunnel" data-id="${t.id}">ویرایش</button></footer></article>`).join(''):'<div class="empty">هنوز سرور ایران و تونلی ثبت نشده است.</div>';$$('.edit-tunnel').forEach(b=>b.onclick=()=>edit(b.dataset.id));$$('.test-tunnel').forEach(b=>b.onclick=()=>test(b.dataset.id))}
  function options(selected=''){return nodes.map(n=>`<option value="${n.id}" ${n.id===selected?'selected':''}>${esc(n.name)} — ${esc(n.provider||'بدون ارائه‌دهنده')}</option>`).join('')}
  function edit(id=null){editing=id?tunnels.find(t=>t.id===id):null;$('#tunnel-title').textContent=editing?'ویرایش تونل':'تونل جدید';$('#tunnel-id').value=editing?.id||'';$('#tunnel-name').value=editing?.name||'';$('#tunnel-transport').value=editing?.transport||'amneziawg';$('#tunnel-entry').innerHTML='<option value="">انتخاب سرور ایران</option>'+options(editing?.entry_node_id);$('#tunnel-exit').innerHTML='<option value="">انتخاب سرور خروجی</option>'+options(editing?.exit_node_id);$('#tunnel-host').value=editing?.public_host||'';$('#tunnel-port').value=editing?.public_port||51820;$('#tunnel-mtu').value=editing?.mtu||1280;$('#tunnel-note').value=editing?.note||'';$('#tunnel-active').checked=Boolean(editing?.active);$('#tunnel-delete').classList.toggle('hidden',!editing);$('#tunnel-error').textContent='';$('#tunnel-dialog').showModal()}
  async function save(e){e.preventDefault();const id=$('#tunnel-id').value,body={name:$('#tunnel-name').value,transport:$('#tunnel-transport').value,entryNodeId:$('#tunnel-entry').value,exitNodeId:$('#tunnel-exit').value,publicHost:$('#tunnel-host').value,publicPort:Number($('#tunnel-port').value),mtu:Number($('#tunnel-mtu').value),note:$('#tunnel-note').value,active:$('#tunnel-active').checked};if(body.entryNodeId===body.exitNodeId){$('#tunnel-error').textContent='ورودی ایران و خروجی خارج باید دو سرور متفاوت باشند.';return}try{await api(id?`/api/admin/transit-tunnels/${id}`:'/api/admin/transit-tunnels',{method:id?'PATCH':'POST',body:JSON.stringify(body)});$('#tunnel-dialog').close();toast('مسیر تونل ذخیره شد');await load()}catch{$('#tunnel-error').textContent='اطلاعات مسیر کامل یا معتبر نیست.'}}
  async function test(id){const b=$(`.test-tunnel[data-id="${id}"]`);b.disabled=true;try{const result=await api(`/api/admin/transit-tunnels/${id}/test`,{method:'POST',body:'{}'});toast(result.status==='online'?`ورودی آنلاین است؛ ${result.latencyMs} میلی‌ثانیه`:'ورودی پاسخ نداد');await load()}catch{toast('آزمایش تونل انجام نشد')}finally{b.disabled=false}}
  async function remove(){if(!editing||!await adminConfirm({title:'حذف تونل',message:'تنظیم ثبت‌شده حذف می‌شود؛ تنظیم سیستم‌عامل سرورها دست‌نخورده می‌ماند.',confirmText:'حذف',danger:true}))return;await api(`/api/admin/transit-tunnels/${editing.id}`,{method:'DELETE'});$('#tunnel-dialog').close();toast('تونل از مدیریت حذف شد');await load()}
  document.addEventListener('DOMContentLoaded',mount);
})();
