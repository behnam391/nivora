# Nivora baseline: keep long-distance TCP responsive under queueing.
# Applied only after confirming tcp_bbr is available on the host kernel.
net.core.default_qdisc = fq
net.ipv4.tcp_congestion_control = bbr
